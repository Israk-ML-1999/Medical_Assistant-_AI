networks:
  app-network:
    driver: bridge

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: doctor_assistant_app
    expose:
      - '8000'
    env_file:
      - .env
    networks:
      - app-network
    restart: unless-stopped 

  nginx:
    image: nginx:alpine
    container_name: nginx_proxy_doctor_assistant_app
    ports:
      - '0.0.0.0:8077:80'
    networks:
      - app-network
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app